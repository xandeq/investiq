---
id: "11-02"
phase: 11-wizard-onde-investir
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - frontend/src/features/wizard/components/WizardContent.tsx
requirements: [WIZ-01, WIZ-05]
autonomous: false
estimated_tasks: 2

must_haves:
  truths:
    - "User sees a 3-step wizard with step progress indicator showing circles 1, 2, 3"
    - "Step 1 shows only the valor disponivel input"
    - "Step 2 shows only the prazo (horizonte) selector buttons"
    - "Step 3 shows only the perfil de risco selector cards and the submit button"
    - "User can click Voltar on steps 2 and 3 to go back without losing input values"
    - "CVM disclaimer amber box is the FIRST child rendered before the step indicator"
    - "After submitting, the processing spinner and results render correctly"
    - "No ticker symbols appear anywhere in the rendered output"
  artifacts:
    - path: "frontend/src/features/wizard/components/WizardContent.tsx"
      provides: "Multi-step wizard with 3 steps, progress indicator, back navigation, CVM disclaimer first"
      min_lines: 200
  key_links:
    - from: "frontend/src/features/wizard/components/WizardContent.tsx"
      to: "frontend/src/features/wizard/hooks/useWizard.ts"
      via: "useWizard() hook for submit, job, status, reset"
      pattern: "useWizard\\(\\)"
    - from: "frontend/src/features/wizard/components/WizardContent.tsx"
      to: "frontend/src/features/wizard/types.ts"
      via: "PrazoLabel, PerfilLabel, WizardDeltaItem, WizardAllocation type imports"
      pattern: "from.*types"
---

<objective>
Refactor WizardContent.tsx from a single-form layout to a 3-step multi-step wizard with a visual step progress indicator and back-navigation, fulfilling WIZ-01.

Purpose: The current WizardContent.tsx renders all three inputs (valor, prazo, perfil) simultaneously. WIZ-01 requires a guided multi-step flow where users progress through one input per step, with a progress indicator and the ability to navigate back without losing data. WIZ-05 requires the CVM disclaimer to remain the first element rendered.

Output: Refactored WizardContent.tsx with 3 steps, progress bar, Voltar buttons, preserved disclaimer placement.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-wizard-onde-investir/11-RESEARCH.md

<interfaces>
<!-- Current WizardContent.tsx state and hooks interface -->

From frontend/src/features/wizard/hooks/useWizard.ts:
```typescript
export function useWizard(): {
  submit: (perfil: PerfilLabel, prazo: PrazoLabel, valor: number) => Promise<void>;
  job: WizardJobResponse | null;
  status: WizardStatus | null;
  isStarting: boolean;
  error: string | null;
  reset: () => void;
}
```

From frontend/src/features/wizard/types.ts:
```typescript
export type PrazoLabel = "6m" | "1a" | "2a" | "5a";
export type PerfilLabel = "conservador" | "moderado" | "arrojado";
export type WizardStatus = "pending" | "running" | "completed" | "failed";
export interface WizardAllocation { acoes_pct: number; fiis_pct: number; renda_fixa_pct: number; caixa_pct: number; rationale: string; }
export interface WizardDeltaItem { asset_class: string; label: string; current_pct: number; suggested_pct: number; delta_pct: number; action: string; valor_delta: number; }
export interface WizardJobResponse { job_id: string; status: WizardStatus; perfil: string; prazo: string; valor: number; disclaimer: string; result: WizardResult | null; error_message: string | null; created_at: string; completed_at: string | null; }
```

Step indicator pattern from RESEARCH.md:
```tsx
function StepIndicator({ current, total }: { current: number; total: number }) {
  return (
    <div className="flex items-center gap-2 mb-6">
      {Array.from({ length: total }, (_, i) => i + 1).map((n) => (
        <div key={n} className="flex items-center">
          <div className={`w-8 h-8 rounded-full flex items-center justify-center text-sm font-bold transition-colors ${
            n < current ? "bg-blue-500 text-white" :
            n === current ? "bg-blue-500 text-white ring-4 ring-blue-100" :
            "bg-gray-200 text-gray-500"
          }`}>{n}</div>
          {n < total && (
            <div className={`h-0.5 w-8 mx-1 ${n < current ? "bg-blue-500" : "bg-gray-200"}`} />
          )}
        </div>
      ))}
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor WizardContent.tsx to 3-step wizard with progress indicator</name>
  <files>frontend/src/features/wizard/components/WizardContent.tsx</files>
  <read_first>
    - frontend/src/features/wizard/components/WizardContent.tsx
    - frontend/src/features/wizard/hooks/useWizard.ts
    - frontend/src/features/wizard/types.ts
    - frontend/src/features/wizard/api.ts
  </read_first>
  <action>
Refactor `WizardContent.tsx` to implement a 3-step wizard. Keep ALL existing sub-components (`AllocationBar`, `DeltaRow`, `fmtBRL`) and ALL existing constants (`PRAZOS`, `PRAZO_LABELS`, `PERFIS`, `AC_COLORS`, `AC_LABELS`, `ACTION_COLORS`, `ACTION_LABELS`) UNCHANGED at the top of the file.

**Add StepIndicator component** (before `WizardContent` export):
```tsx
function StepIndicator({ current, total }: { current: number; total: number }) {
  const STEP_LABELS = ["Valor", "Prazo", "Perfil"];
  return (
    <div className="flex items-center gap-2 mb-6">
      {Array.from({ length: total }, (_, i) => i + 1).map((n) => (
        <div key={n} className="flex items-center">
          <div className="flex flex-col items-center">
            <div className={`w-8 h-8 rounded-full flex items-center justify-center text-sm font-bold transition-colors ${
              n < current ? "bg-blue-500 text-white" :
              n === current ? "bg-blue-500 text-white ring-4 ring-blue-100" :
              "bg-gray-200 text-gray-500"
            }`}>{n}</div>
            <span className="text-xs text-gray-500 mt-1">{STEP_LABELS[n - 1]}</span>
          </div>
          {n < total && (
            <div className={`h-0.5 w-8 mx-1 mb-5 ${n < current ? "bg-blue-500" : "bg-gray-200"}`} />
          )}
        </div>
      ))}
    </div>
  );
}
```

**Modify WizardContent state** -- add step state:
```tsx
const [step, setStep] = useState<1 | 2 | 3>(1);
```
Keep existing `valorInput`, `prazo`, `perfil` state and `useWizard()` hook UNCHANGED.

**Restructure the form section** `{(isIdle || isFailed) && (...)}`:

Replace the single card containing all 3 inputs with a stepped layout:

```tsx
{(isIdle || isFailed) && (
  <>
    <StepIndicator current={step} total={3} />
    <div className="rounded-xl border border-gray-200 bg-white p-5 space-y-5">

      {/* Step 1: Valor disponivel */}
      {step === 1 && (
        <div>
          <h2 className="text-sm font-bold text-gray-800 mb-3">Quanto voce quer investir?</h2>
          <label className="block text-xs font-semibold text-gray-600 mb-1.5">
            Valor disponivel para investir (R$)
          </label>
          <input
            type="number"
            min={100}
            value={valorInput}
            onChange={(e) => setValorInput(e.target.value)}
            placeholder="Ex: 10000"
            className="w-44 rounded-lg border border-gray-200 px-3 py-2 text-sm focus:outline-none focus:border-blue-400"
          />
          <p className="text-xs text-gray-400 mt-1">Minimo: R$ 100</p>
          <div className="flex justify-end mt-4">
            <button
              onClick={() => setStep(2)}
              disabled={!valorInput || parseFloat(valorInput) < 100}
              className="px-6 py-2.5 rounded-lg bg-blue-500 text-white text-sm font-semibold hover:bg-blue-600 disabled:opacity-50 transition-colors"
            >
              Proximo
            </button>
          </div>
        </div>
      )}

      {/* Step 2: Prazo (horizonte) */}
      {step === 2 && (
        <div>
          <h2 className="text-sm font-bold text-gray-800 mb-3">Qual o prazo do investimento?</h2>
          <label className="block text-xs font-semibold text-gray-600 mb-1.5">Horizonte de investimento</label>
          <div className="flex rounded-lg border border-gray-200 overflow-hidden w-fit">
            {PRAZOS.map((p) => (
              <button
                key={p}
                onClick={() => setPrazo(p)}
                className={`px-5 py-2 text-sm font-medium transition-colors ${
                  prazo === p
                    ? "bg-blue-500 text-white"
                    : "bg-white text-gray-600 hover:bg-gray-50"
                }`}
              >
                {PRAZO_LABELS[p]}
              </button>
            ))}
          </div>
          <div className="flex justify-between mt-4">
            <button
              onClick={() => setStep(1)}
              className="px-6 py-2.5 rounded-lg border border-gray-200 text-gray-600 text-sm font-semibold hover:bg-gray-50 transition-colors"
            >
              Voltar
            </button>
            <button
              onClick={() => setStep(3)}
              className="px-6 py-2.5 rounded-lg bg-blue-500 text-white text-sm font-semibold hover:bg-blue-600 transition-colors"
            >
              Proximo
            </button>
          </div>
        </div>
      )}

      {/* Step 3: Perfil de risco + submit */}
      {step === 3 && (
        <div>
          <h2 className="text-sm font-bold text-gray-800 mb-3">Qual o seu perfil de risco?</h2>
          <label className="block text-xs font-semibold text-gray-600 mb-1.5">Perfil de risco</label>
          <div className="grid grid-cols-1 sm:grid-cols-3 gap-3">
            {PERFIS.map((p) => {
              const isSelected = perfil === p.key;
              const borderMap: Record<string, string> = {
                emerald: isSelected ? "border-emerald-500 bg-emerald-50" : "border-gray-200 hover:border-emerald-300",
                blue:    isSelected ? "border-blue-500 bg-blue-50"       : "border-gray-200 hover:border-blue-300",
                purple:  isSelected ? "border-purple-500 bg-purple-50"   : "border-gray-200 hover:border-purple-300",
              };
              const textMap: Record<string, string> = {
                emerald: isSelected ? "text-emerald-700" : "text-gray-700",
                blue:    isSelected ? "text-blue-700"    : "text-gray-700",
                purple:  isSelected ? "text-purple-700"  : "text-gray-700",
              };
              return (
                <button
                  key={p.key}
                  onClick={() => setPerfil(p.key)}
                  className={`border-2 rounded-xl p-4 text-left transition-all ${borderMap[p.color]}`}
                >
                  <p className={`text-sm font-bold ${textMap[p.color]}`}>{p.label}</p>
                  <p className="text-xs text-gray-500 mt-1">{p.desc}</p>
                </button>
              );
            })}
          </div>
          <div className="flex justify-between mt-4">
            <button
              onClick={() => setStep(2)}
              className="px-6 py-2.5 rounded-lg border border-gray-200 text-gray-600 text-sm font-semibold hover:bg-gray-50 transition-colors"
            >
              Voltar
            </button>
            <button
              onClick={handleSubmit}
              disabled={!valorInput || parseFloat(valorInput) < 100}
              className="px-6 py-2.5 rounded-lg bg-blue-500 text-white text-sm font-semibold hover:bg-blue-600 disabled:opacity-50 transition-colors"
            >
              Analisar onde investir
            </button>
          </div>

          {isFailed && (
            <p className="text-sm text-red-600 mt-3">
              {job?.error_message ?? error ?? "Erro ao processar recomendacao. Tente novamente."}
            </p>
          )}
        </div>
      )}
    </div>
  </>
)}
```

**Critical rules:**
1. The CVM disclaimer `<div className="rounded-lg bg-amber-50 ...">` MUST remain the FIRST child of the outer `<div className="space-y-6">` -- before StepIndicator, before form, before results. Do NOT move it.
2. All state (`step`, `valorInput`, `prazo`, `perfil`) lives in the single `WizardContent` parent. Do NOT create sub-components with their own state -- this prevents state reset on back-navigation.
3. When user clicks "Nova analise" (reset button at the bottom of results), also reset step back to 1: modify the reset button `onClick` to `() => { reset(); setStep(1); }`.
4. The `isProcessing` and `isDone` sections remain UNCHANGED -- they already render correctly.
5. The `StepIndicator` is only shown when `isIdle || isFailed` (form is active), NOT during processing or results.
  </action>
  <verify>
    <automated>cd D:/_DEV/claude-code/financas/frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "StepIndicator" frontend/src/features/wizard/components/WizardContent.tsx returns 0
    - grep -q 'useState<1 | 2 | 3>(1)' frontend/src/features/wizard/components/WizardContent.tsx returns 0
    - grep -c "Voltar" frontend/src/features/wizard/components/WizardContent.tsx returns 2 (one in step 2, one in step 3)
    - grep -c "Proximo" frontend/src/features/wizard/components/WizardContent.tsx returns 2 (one in step 1, one in step 2)
    - grep -q "setStep(1)" frontend/src/features/wizard/components/WizardContent.tsx returns 0 (back from step 2 + reset)
    - grep -q "setStep(2)" frontend/src/features/wizard/components/WizardContent.tsx returns 0 (advance from step 1 + back from step 3)
    - grep -q "setStep(3)" frontend/src/features/wizard/components/WizardContent.tsx returns 0 (advance from step 2)
    - The CVM disclaimer div (bg-amber-50) appears BEFORE any StepIndicator or form content in the JSX -- verified by reading the file and confirming the amber div is the first child of space-y-6
    - grep -q "AllocationBar" frontend/src/features/wizard/components/WizardContent.tsx returns 0 (component preserved)
    - grep -q "DeltaRow" frontend/src/features/wizard/components/WizardContent.tsx returns 0 (component preserved)
    - TypeScript compilation passes with no errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>WizardContent.tsx refactored to 3-step wizard with StepIndicator, Voltar/Proximo navigation, CVM disclaimer first, all existing result rendering preserved</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify multi-step wizard UI and CVM disclaimer placement</name>
  <files>frontend/src/features/wizard/components/WizardContent.tsx</files>
  <action>
Human visual verification of the multi-step wizard UI. No code changes -- this is a review checkpoint.

Steps to verify:
1. Navigate to the wizard page (http://localhost:3000/wizard or the VPS URL)
2. Verify the amber CVM disclaimer box is the FIRST element on the page -- above everything else
3. Verify step indicator shows 3 circles labeled "1 Valor", "2 Prazo", "3 Perfil" -- circle 1 should be highlighted blue
4. Enter a valor (e.g. 10000) and click "Proximo" -- step indicator should advance to step 2
5. On step 2, verify prazo buttons (6 meses, 1 ano, 2 anos, 5 anos) are shown. Select one. Click "Proximo"
6. On step 3, verify 3 perfil cards (Conservador, Moderado, Arrojado) are shown with the submit button "Analisar onde investir"
7. Click "Voltar" on step 3 -- verify step 2 shows with your previously selected prazo still selected
8. Click "Voltar" on step 2 -- verify step 1 shows with your previously entered valor still in the input
9. Navigate forward to step 3 again and click "Analisar onde investir"
10. Verify the processing spinner appears with perfil/prazo/valor badges
11. When results load, verify: allocation bar, percentage cards, rationale, and delta section all render
12. Verify NO ticker symbols (PETR4, VALE3, etc.) appear anywhere in the results
13. Click "Nova analise" -- verify the form resets to step 1
  </action>
  <verify>Human visual inspection -- type "approved" or describe issues</verify>
  <done>Human confirms: 3-step wizard renders correctly, CVM disclaimer is first, back-navigation preserves inputs, results display correctly with no tickers</done>
</task>

</tasks>

<verification>
```bash
# TypeScript compiles without errors
cd D:/_DEV/claude-code/financas/frontend && npx tsc --noEmit

# Backend tests still pass
cd D:/_DEV/claude-code/financas/backend && python -m pytest tests/test_wizard.py -x -v

# Full backend suite green
cd D:/_DEV/claude-code/financas/backend && python -m pytest tests/ -x -q
```
</verification>

<success_criteria>
- WizardContent.tsx implements 3-step wizard (step 1: valor, step 2: prazo, step 3: perfil + submit)
- Step progress indicator with 3 labeled circles renders correctly
- Back-navigation (Voltar) preserves all input values
- CVM disclaimer is the first rendered element (before step indicator and form)
- All existing result rendering (AllocationBar, DeltaRow, macro, rationale) works unchanged
- TypeScript compiles with no errors
- Human approves the visual UI via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/11-wizard-onde-investir/11-02-SUMMARY.md`
</output>
