---
phase: 12-foundation-legal-cost-control-async-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0020_add_analysis_tables.py
  - backend/app/modules/analysis/__init__.py
  - backend/app/modules/analysis/models.py
  - backend/app/modules/analysis/schemas.py
  - backend/app/modules/analysis/versioning.py
  - backend/app/modules/analysis/constants.py
  - backend/tests/test_phase12_foundation.py
  - backend/tests/fixtures/analysis_fixtures.py
autonomous: true
requirements:
  - AI-08
  - AI-09
  - AI-11
  - AI-12

must_haves:
  truths:
    - "Analysis module exists with models for jobs, quota logs, and cost logs"
    - "Alembic migration creates all three tables and can be applied cleanly"
    - "Pydantic schemas define the API response contract with data versioning metadata"
    - "Test scaffold exists with fixtures for free/pro/enterprise users and sample ticker data"
  artifacts:
    - path: "backend/app/modules/analysis/models.py"
      provides: "AnalysisJob, AnalysisQuotaLog, AnalysisCostLog SQLAlchemy models"
      contains: "class AnalysisJob"
    - path: "backend/app/modules/analysis/schemas.py"
      provides: "DataMetadata, AnalysisResponse, DCFRequest Pydantic models"
      exports: ["DataMetadata", "AnalysisResponse"]
    - path: "backend/alembic/versions/0020_add_analysis_tables.py"
      provides: "Migration creating analysis_jobs, analysis_quota_logs, analysis_cost_logs"
      contains: "create_table"
    - path: "backend/tests/test_phase12_foundation.py"
      provides: "Test scaffold with quota, versioning, async job test stubs"
      min_lines: 80
  key_links:
    - from: "backend/app/modules/analysis/models.py"
      to: "backend/app/modules/auth/models.py"
      via: "tenant_id foreign key pattern (same as WizardJob)"
      pattern: "tenant_id.*String\\(36\\)"
    - from: "backend/app/modules/analysis/schemas.py"
      to: "backend/app/modules/analysis/models.py"
      via: "Schema mirrors model fields"
      pattern: "class AnalysisResponse"
---

<objective>
Create the analysis module foundation: database models, Alembic migration, Pydantic schemas, data versioning utilities, and test scaffold for Phase 12.

Purpose: All subsequent plans (quota enforcement, async jobs, rate limiting) depend on these models and schemas existing. This is the contract-first foundation.
Output: New `backend/app/modules/analysis/` module with models, schemas, versioning utils, and a test file with fixtures ready for Plans 02 and 03.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\claude-code\investiq\.planning\PROJECT.md
@D:\claude-code\investiq\.planning\ROADMAP.md
@D:\claude-code\investiq\.planning\STATE.md
@D:\claude-code\investiq\.planning\phases\12-foundation-legal-cost-control-async-architecture\12-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From backend/app/modules/wizard/models.py (pattern to replicate for AnalysisJob):
```python
class WizardJob(Base):
    __tablename__ = "wizard_jobs"
    id: Mapped[str] = mapped_column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    tenant_id: Mapped[str] = mapped_column(String(36), nullable=False)
    status: Mapped[str] = mapped_column(String(20), nullable=False, default="pending")
    result_json: Mapped[str | None] = mapped_column(Text, nullable=True)
    error_message: Mapped[str | None] = mapped_column(String(500), nullable=True)
    retry_count: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=_utcnow)
    completed_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
```

From backend/app/modules/auth/models.py:
```python
class User(Base):
    tenant_id: Mapped[str] = mapped_column(String(36), nullable=False, index=True)
    # User.plan is "free" | "pro" (canonical access-control field)
```

From backend/app/core/plan_gate.py:
```python
async def get_user_plan(...) -> str:
    # Returns "free" | "pro" | "enterprise"
    # Trial elevation: free user with active trial gets "pro"
```

From backend/app/modules/billing/models.py:
```python
class Subscription(Base):
    plan: Mapped[str] = mapped_column(String(50), nullable=False)  # "free" | "pro"
```

Latest migration: `0019_add_ai_usage_logs.py`
DB sync helpers: `get_sync_db_session(tenant_id)`, `get_superuser_sync_db_session()`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analysis module with models, schemas, versioning, and constants</name>
  <files>
    backend/app/modules/analysis/__init__.py
    backend/app/modules/analysis/models.py
    backend/app/modules/analysis/schemas.py
    backend/app/modules/analysis/versioning.py
    backend/app/modules/analysis/constants.py
  </files>
  <read_first>
    backend/app/modules/wizard/models.py
    backend/app/modules/auth/models.py
    backend/app/modules/billing/models.py
    backend/app/core/plan_gate.py
    backend/app/core/db.py
  </read_first>
  <action>
Create the `backend/app/modules/analysis/` package with 5 files:

**__init__.py** — Empty init file.

**models.py** — Three SQLAlchemy models following the WizardJob pattern:

1. `AnalysisJob(Base)` with `__tablename__ = "analysis_jobs"`:
   - `id`: `String(36)`, primary key, `default=lambda: str(uuid.uuid4())`
   - `tenant_id`: `String(36)`, nullable=False
   - `analysis_type`: `String(50)`, nullable=False (values: "dcf", "earnings", "dividend", "sector")
   - `ticker`: `String(10)`, nullable=False
   - `data_timestamp`: `DateTime(timezone=True)`, nullable=False
   - `data_version_id`: `String(100)`, nullable=False (e.g. "brapi_eod_20260331_v1.2")
   - `data_sources`: `Text`, nullable=True (JSON array string)
   - `status`: `String(20)`, nullable=False, default="pending" (values: "pending", "running", "completed", "failed", "stale")
   - `result_json`: `Text`, nullable=True
   - `error_message`: `String(500)`, nullable=True
   - `retry_count`: `Integer`, default=0
   - `created_at`: `DateTime(timezone=True)`, default=_utcnow
   - `completed_at`: `DateTime(timezone=True)`, nullable=True
   - Indexes: `ix_analysis_jobs_tenant_status` on (tenant_id, status), `ix_analysis_jobs_tenant_ticker` on (tenant_id, ticker), `ix_analysis_jobs_created` on (created_at), `ix_analysis_jobs_data_version` on (data_version_id)

2. `AnalysisQuotaLog(Base)` with `__tablename__ = "analysis_quota_logs"`:
   - `id`: `String(36)`, primary key, `default=lambda: str(uuid.uuid4())`
   - `tenant_id`: `String(36)`, nullable=False
   - `year_month`: `String(7)`, nullable=False (format "2026-03")
   - `plan_tier`: `String(20)`, nullable=False (values: "free", "pro", "enterprise")
   - `quota_limit`: `Integer`, nullable=False
   - `quota_used`: `Integer`, nullable=False, default=0
   - Index: `ix_quota_tenant_month` on (tenant_id, year_month), unique=True

3. `AnalysisCostLog(Base)` with `__tablename__ = "analysis_cost_logs"`:
   - `id`: `String(36)`, primary key, `default=lambda: str(uuid.uuid4())`
   - `tenant_id`: `String(36)`, nullable=False
   - `job_id`: `String(36)`, nullable=False
   - `analysis_type`: `String(50)`, nullable=False
   - `ticker`: `String(10)`, nullable=False
   - `llm_provider`: `String(50)`, nullable=True
   - `llm_model`: `String(100)`, nullable=True
   - `input_tokens`: `Integer`, nullable=True
   - `output_tokens`: `Integer`, nullable=True
   - `estimated_cost_usd`: `Numeric(10, 4)`, nullable=True
   - `duration_ms`: `Integer`, nullable=False
   - `status`: `String(20)`, nullable=False
   - `created_at`: `DateTime(timezone=True)`, default=_utcnow
   - Indexes: `ix_cost_tenant_created` on (tenant_id, created_at), `ix_cost_analysis_type` on (analysis_type)

Use the same import pattern as wizard/models.py for `Base`, `_utcnow`, `Mapped`, `mapped_column`, etc.

**schemas.py** — Pydantic v2 models:

1. `DataMetadata(BaseModel)`:
   - `data_timestamp: datetime`
   - `data_version_id: str`
   - `data_sources: list[dict]` (each dict: {"source": str, "type": str, "freshness": str})
   - `cache_hit: bool`
   - `cache_ttl_seconds: int`

2. `AnalysisResponse(BaseModel)`:
   - `analysis_id: str`
   - `analysis_type: str`
   - `ticker: str`
   - `status: str`
   - `result: dict | None = None`
   - `data_metadata: DataMetadata | None = None`
   - `disclaimer: str`
   - `error_message: str | None = None`

3. `DCFRequest(BaseModel)`:
   - `ticker: str` (min_length=4, max_length=10)
   - `growth_rate: float | None = None` (ge=0, le=0.20)
   - `discount_rate: float | None = None` (ge=0, le=0.30)
   - `terminal_growth: float | None = None` (ge=0, le=0.05)

4. `AnalysisJobStatus(BaseModel)`:
   - `job_id: str`
   - `status: str`
   - `message: str | None = None`

**versioning.py** — Data versioning utilities:

1. `build_data_version_id() -> str`: Returns `f"brapi_eod_{datetime.utcnow().strftime('%Y%m%d')}_v1.2"`
2. `get_data_sources() -> list[dict]`: Returns the two-item list from RESEARCH.md Section 3.1 (BRAPI fundamentals 1d, B3/CVM financial_statements 1q)

**constants.py** — Shared constants:

1. `ANALYSIS_TYPES = ("dcf", "earnings", "dividend", "sector")`
2. `QUOTA_LIMITS = {"free": 0, "pro": 50, "enterprise": 500}`
3. `CVM_DISCLAIMER_PT = "Analise informativa, nao constitui recomendacao de investimento pessoal. O conteudo e apresentado unicamente para fins educacionais e informativos, baseado em dados historicos e metodologias de valuation amplamente reconhecidas. Cada investidor tem sua propria situacao financeira, tolerancia ao risco e objetivos. Consulte um assessor financeiro registrado na CVM se precisar de recomendacao customizada. (CVM Res. 19/2021, Res. 30/2021)"`
   Note: Use proper Portuguese accents (UTF-8): "Analise" -> "Analise informativa" with proper accents as in RESEARCH.md Section 1.1
4. `CVM_DISCLAIMER_SHORT_PT = "Analise informativa, nao constitui recomendacao de investimento pessoal (CVM Res. 19/2021)"` (with proper accents)
  </action>
  <verify>
    <automated>cd D:/claude-code/investiq && python -c "from backend.app.modules.analysis.models import AnalysisJob, AnalysisQuotaLog, AnalysisCostLog; from backend.app.modules.analysis.schemas import DataMetadata, AnalysisResponse, DCFRequest; from backend.app.modules.analysis.versioning import build_data_version_id; from backend.app.modules.analysis.constants import QUOTA_LIMITS, CVM_DISCLAIMER_PT; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/modules/analysis/__init__.py exists
    - backend/app/modules/analysis/models.py contains `class AnalysisJob(Base)` and `class AnalysisQuotaLog(Base)` and `class AnalysisCostLog(Base)`
    - backend/app/modules/analysis/models.py contains `__tablename__ = "analysis_jobs"` and `__tablename__ = "analysis_quota_logs"` and `__tablename__ = "analysis_cost_logs"`
    - backend/app/modules/analysis/models.py contains `data_version_id` and `data_timestamp` and `data_sources` fields on AnalysisJob
    - backend/app/modules/analysis/schemas.py contains `class DataMetadata(BaseModel)` and `class AnalysisResponse(BaseModel)` and `class DCFRequest(BaseModel)`
    - backend/app/modules/analysis/schemas.py contains `disclaimer: str` field on AnalysisResponse
    - backend/app/modules/analysis/versioning.py contains `def build_data_version_id` and `def get_data_sources`
    - backend/app/modules/analysis/constants.py contains `QUOTA_LIMITS = {"free": 0, "pro": 50, "enterprise": 500}`
    - backend/app/modules/analysis/constants.py contains `CVM_DISCLAIMER_PT` and `CVM_DISCLAIMER_SHORT_PT`
    - All Python imports succeed without errors
  </acceptance_criteria>
  <done>Analysis module package created with 3 SQLAlchemy models, 4 Pydantic schemas, versioning utils, and constants. All imports succeed.</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration 0020 and test scaffold with fixtures</name>
  <files>
    backend/alembic/versions/0020_add_analysis_tables.py
    backend/tests/test_phase12_foundation.py
    backend/tests/fixtures/analysis_fixtures.py
  </files>
  <read_first>
    backend/alembic/versions/0019_add_ai_usage_logs.py
    backend/app/modules/analysis/models.py
    backend/tests/conftest.py
    backend/app/modules/wizard/models.py
  </read_first>
  <action>
**Migration 0020** (`backend/alembic/versions/0020_add_analysis_tables.py`):

- `revision = "0020"` (or auto-generated hash — follow the pattern from 0019)
- `down_revision` = revision ID from 0019 (read 0019 to get its exact revision hash)
- Create three tables in `upgrade()`:

1. `analysis_jobs`:
   - `id` VARCHAR(36) PRIMARY KEY
   - `tenant_id` VARCHAR(36) NOT NULL
   - `analysis_type` VARCHAR(50) NOT NULL
   - `ticker` VARCHAR(10) NOT NULL
   - `data_timestamp` TIMESTAMP WITH TIME ZONE NOT NULL
   - `data_version_id` VARCHAR(100) NOT NULL
   - `data_sources` TEXT
   - `status` VARCHAR(20) NOT NULL DEFAULT 'pending'
   - `result_json` TEXT
   - `error_message` VARCHAR(500)
   - `retry_count` INTEGER NOT NULL DEFAULT 0
   - `created_at` TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
   - `completed_at` TIMESTAMP WITH TIME ZONE
   - Indexes: `ix_analysis_jobs_tenant_status(tenant_id, status)`, `ix_analysis_jobs_tenant_ticker(tenant_id, ticker)`, `ix_analysis_jobs_created(created_at)`, `ix_analysis_jobs_data_version(data_version_id)`

2. `analysis_quota_logs`:
   - `id` VARCHAR(36) PRIMARY KEY
   - `tenant_id` VARCHAR(36) NOT NULL
   - `year_month` VARCHAR(7) NOT NULL
   - `plan_tier` VARCHAR(20) NOT NULL
   - `quota_limit` INTEGER NOT NULL
   - `quota_used` INTEGER NOT NULL DEFAULT 0
   - Unique index: `ix_quota_tenant_month(tenant_id, year_month)`

3. `analysis_cost_logs`:
   - `id` VARCHAR(36) PRIMARY KEY
   - `tenant_id` VARCHAR(36) NOT NULL
   - `job_id` VARCHAR(36) NOT NULL
   - `analysis_type` VARCHAR(50) NOT NULL
   - `ticker` VARCHAR(10) NOT NULL
   - `llm_provider` VARCHAR(50)
   - `llm_model` VARCHAR(100)
   - `input_tokens` INTEGER
   - `output_tokens` INTEGER
   - `estimated_cost_usd` NUMERIC(10, 4)
   - `duration_ms` INTEGER NOT NULL
   - `status` VARCHAR(20) NOT NULL
   - `created_at` TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
   - Indexes: `ix_cost_tenant_created(tenant_id, created_at)`, `ix_cost_analysis_type(analysis_type)`

`downgrade()` drops all three tables in reverse order.

**Test fixtures** (`backend/tests/fixtures/analysis_fixtures.py`):

Create pytest fixtures:
- `sample_tickers() -> list[str]`: Returns `["PETR4", "VALE3", "BBDC4", "ITUB4", "WEGE3"]`
- `mock_brapi_fundamentals(ticker: str) -> dict`: Returns dict with `current_price`, `eps`, `pe_ratio`, `dividend_yield`, `free_cash_flow`, `revenue`, `market_cap` for each sample ticker (use realistic but static values)
- `mock_analysis_job(tenant_id: str, ticker: str, analysis_type: str) -> dict`: Returns dict matching AnalysisJob constructor kwargs with status="pending", generated uuid, data_version_id from versioning.build_data_version_id()

**Test scaffold** (`backend/tests/test_phase12_foundation.py`):

Create test file with the following test functions (initially as stubs with `pass` body that will be filled in Plans 02 and 03, EXCEPT the model/schema tests which should be fully implemented):

Fully implemented tests:
- `test_analysis_job_model_fields()`: Assert AnalysisJob has all required columns (data_version_id, data_timestamp, data_sources, status, tenant_id, ticker, analysis_type)
- `test_analysis_quota_log_model_fields()`: Assert AnalysisQuotaLog has tenant_id, year_month, plan_tier, quota_limit, quota_used
- `test_analysis_cost_log_model_fields()`: Assert AnalysisCostLog has job_id, analysis_type, llm_provider, estimated_cost_usd
- `test_data_version_id_format()`: Call `build_data_version_id()` and assert result matches pattern `r"brapi_eod_\d{8}_v1\.2"`
- `test_dcf_request_validation()`: Create DCFRequest with valid and invalid inputs, assert validation works (growth_rate > 0.20 fails, ticker min_length=4)
- `test_analysis_response_includes_disclaimer()`: Create AnalysisResponse, assert `disclaimer` field is present and non-empty
- `test_quota_limits_constants()`: Assert `QUOTA_LIMITS["free"] == 0`, `QUOTA_LIMITS["pro"] == 50`, `QUOTA_LIMITS["enterprise"] == 500`

Stub tests (body = `pytest.skip("Implemented in Plan 02/03")`):
- `test_quota_enforcement_free_tier_blocks_requests`
- `test_quota_enforcement_pro_tier_allows_50_per_month`
- `test_rate_limiting_middleware_enforced`
- `test_cost_tracking_per_analysis_type`
- `test_analysis_includes_data_version_id`
- `test_data_timestamp_visible_in_api`
- `test_async_job_lifecycle_complete`
- `test_celery_task_error_handling`
- `test_api_response_includes_disclaimer`
- `test_disclaimer_component_renders`
  </action>
  <verify>
    <automated>cd D:/claude-code/investiq && python -m pytest backend/tests/test_phase12_foundation.py -v -k "model_fields or version_id_format or dcf_request or response_includes_disclaimer or quota_limits_constants" --no-header -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/alembic/versions/0020_add_analysis_tables.py exists and contains `op.create_table("analysis_jobs"` and `op.create_table("analysis_quota_logs"` and `op.create_table("analysis_cost_logs"`
    - Migration down_revision matches the revision from 0019
    - backend/tests/test_phase12_foundation.py contains at least 17 test functions (7 implemented + 10 stubs)
    - backend/tests/test_phase12_foundation.py contains `from backend.app.modules.analysis.models import AnalysisJob` (or equivalent import path)
    - The 7 implemented tests pass (model fields, version_id format, DCF request validation, response disclaimer, quota limits constants)
    - backend/tests/fixtures/analysis_fixtures.py contains `sample_tickers` and `mock_brapi_fundamentals`
  </acceptance_criteria>
  <done>Migration 0020 creates 3 tables with correct columns and indexes. Test scaffold has 7 passing tests and 10 stubs for Plans 02/03. Fixtures provide sample ticker data.</done>
</task>

</tasks>

<verification>
- All 5 analysis module files exist and import cleanly
- Migration 0020 is syntactically valid and references correct down_revision
- 7 unit tests pass for models, schemas, versioning, and constants
- 10 stub tests are marked as skip (not failing)
- No import errors from existing modules
</verification>

<success_criteria>
- `python -c "from backend.app.modules.analysis.models import AnalysisJob"` succeeds
- `pytest backend/tests/test_phase12_foundation.py -k "model_fields or version_id or dcf_request or disclaimer or quota_limits"` shows 7 passed
- Migration file exists with correct table definitions
</success_criteria>

<output>
After completion, create `.planning/phases/12-foundation-legal-cost-control-async-architecture/12-01-SUMMARY.md`
</output>
